@Java
method surroundWithFlowers()
{
   plant();
   while(!isFlower(AHEAD))
   {
      if (isWater(AHEAD))
         turn(RIGHT);
      hop();
      plant();
   }
}
@@
// Lab 4D

// using blank

method main()
{
   Jeroo Joey = new Jeroo(92);
   
   Joey.surroundWithFlowers();
}
